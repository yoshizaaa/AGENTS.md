# Project Map

This file records where important parts of the Pluto project live.

## Current State

No app has been scaffolded yet.

## Planned Locations

| Area | Planned Path | Status |
| --- | --- | --- |
| Admin dashboard | `apps/admin` | Not created |
| Backend routes | `apps/api` or `apps/web/src/app/api` | Not created |
| Database schema | `packages/db/schema` | Not created |
| Database migrations | `packages/db/migrations` | Not created |
| Shared UI | `packages/ui` | Not created |
| Design docs | `docs` | Created |

## Environment Loading

Not configured yet.

When an app is added, document:

- Which `.env` files are loaded
- Which framework loads them
- Required variables
- Whether variables are server-only or client-exposed

## Local Run Commands

Not configured yet.

When an app is added, document:

```bash
npm install
npm run dev
```

or the actual package manager commands used by the repo.

